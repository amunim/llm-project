# NUST Bank RAG – backend dependencies
#
# Local Windows (CPU):  pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# HF Spaces GPU (CUDA): llama-cpp-python is installed separately in Dockerfile (cu121 wheel; pin matches index).
#                        DO NOT add llama-cpp-python here – it would overwrite the CUDA wheel with a CPU one.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6

# HuggingFace Hub (model download)
huggingface_hub==0.27.1

# RAG / vector store
langchain==0.3.18
langchain-community==0.3.17
langchain-huggingface==0.1.2
chromadb==0.6.3
sentence-transformers==3.3.1

# Ingest: PDF / Word
pypdf==5.1.0
python-docx==1.1.2
